\input{skripte/meta}

\usepackage[
backend=biber,
style=ieee,
mincitenames=1,
maxcitenames=1,	% mindestens 1 Name ausgeben bevor et. al. kommt
minbibnames=1,
maxbibnames=6, % maximal 6 nehmen werden ausgegeben
date=iso,
seconds=true, %werden nicht verwendet, so werden aber Warnungen unterdrückt.
urldate=iso,
dashed=false,
autocite=inline,
useprefix=true, % 'von' im Namen beachten (beim Anzeigen)
mincrossrefs = 1
]{biblatex}%iso dateformat für YYYY-MM-DD

%weitere Anpassungen für BibLaTex
%\input{skripte/modsBiblatex2018}
\DefineBibliographyStrings{ngerman}{ 
	andothers = {{et\,al\adddot}},             
}
\DefineBibliographyStrings{english}{ 
	andothers = {{et\,al\adddot}},             
}

\addbibresource{literature.bib}
\AtBeginBibliography{\singlespacing}


\usepackage{hyphsubst}
\HyphSubstIfExists{ngerman-x-latest}{%
\HyphSubstLet{ngerman}{ngerman-x-latest}}{}
\usepackage{microtype}
\usepackage[%
left = \glqq,%
right = \grqq,%
leftsub = \glq,%
rightsub = \grq%
]{dirtytalk}
\hyphenpenalty=5000 % Minimiert die Trennung
\exhyphenpenalty=5000 % Minimiert die Trennung am Zeilenende

\usepackage{url}
\urlstyle{same}

\usepackage{setspace}
\onehalfspacing
\setlength{\parindent}{0mm}
\setlength{\parskip}{0.8em plus 0.5em minus 0.3em}
\sloppy					%Abstände variieren